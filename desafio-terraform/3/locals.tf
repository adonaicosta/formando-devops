locals {  
    iterator = {          
      for i in range(100) :
      condition (100%div == 0 ? div_list[div_index] = i &&
      div_index = div + 1 &&
      index = index + 1:
      index = index + 1)       
      }
}